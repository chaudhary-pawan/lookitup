# ── API ───────────────────────────────────────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.29.0
python-multipart==0.0.9        # file uploads

# ── ML / CV ───────────────────────────────────────────────────────────────────
insightface==0.7.3
onnxruntime==1.17.3            # InsightFace inference backend
opencv-python-headless==4.9.0.80
numpy==1.26.4

# ── Vector Search ─────────────────────────────────────────────────────────────
# Replaced FAISS with pgvector for production vector search
# ── Database ──────────────────────────────────────────────────────────────────
sqlalchemy==2.0.30
alembic==1.13.1
aiosqlite==0.20.0              # async SQLite driver for dev
asyncpg==0.29.0                # async Postgres driver for prod
pgvector==0.2.5                # Postgres vector extension for SQLAlchemy

# ── Async task queue ──────────────────────────────────────────────────────────
celery==5.3.6
redis==5.0.4

# ── Storage ───────────────────────────────────────────────────────────────────
cloudinary==1.40.0             # optional: only needed for cloud storage
boto3                          # optional: only needed for S3 (unpinned — install separately if needed)

# ── Security & Utilities ──────────────────────────────────────────────────────
passlib[bcrypt]==1.7.4         # password hashing
PyJWT==2.8.0                   # JWT tokens
python-dotenv==1.0.1
pydantic==2.7.1
pydantic-settings==2.2.1
Pillow==10.3.0
aiofiles==23.2.1
httpx==0.27.0                  # for async internal requests (optional)

# ── Dev / Testing ─────────────────────────────────────────────────────────────
pytest==8.2.0
pytest-asyncio==0.23.6
httpx==0.27.0                  # test client for FastAPI
